package config

// Commander configuration, created once per configured commander
type Commander struct {

	// ListenAddress defines the the local network address this commander
	// shall listen on.
	ListenAddress string

}